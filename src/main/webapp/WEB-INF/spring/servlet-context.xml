<?xml version="1.0" encoding="UTF-8"?>
<!-- <mvc:annotation-driven> 같은 MVC 전용 태그를 쓰겠다는 선언  -->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- @Controller 들이 들어있는 패키지 -->
    <!-- com.meeting.summary.controller 패키지 안에서 @Controller를 찾아서
	스프링 빈으로 등록  -->
    <context:component-scan base-package="com.meeting.summary.controller"/>

    <!-- @GetMapping, @PostMapping 등 사용 가능하게 설정 -->
    <mvc:annotation-driven/>

    <!-- 뷰 리졸버 : return "home"; 이라고 하면
	실제로는 /WEB-INF/views/home.jsp 파일을 찾아가게 만들어주는 역할 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

</beans>
