<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <display-name>meeting-summary</display-name>

    <!-- 한글 깨짐 방지 필터 -->
    <filter>
        <filter-name>encodingFilter</filter-name>
        <!-- 모든 요청/응답의 문자 인코딩을 UTF-8로 통일  -->
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>encodingFilter</filter-name>
        <!-- 이 필터를 모든 URL에 적용  -->
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Root Spring Context -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <!-- 스프링 설정 파일 경로  -->
        <param-value>/WEB-INF/spring/root-context.xml</param-value>
    </context-param>

    <listener>
    	<!-- 웹앱이 시작될 때 스프링 컨테이너(Bean들 관리하는 공장)를 먼저 하나 띄워주는 리스너  -->
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <!-- DispatcherServlet (컨트롤러 + 뷰 담당) -->
    <servlet>
        <servlet-name>appServlet</servlet-name>
        <!-- 모든 웹 요청을 이 서블릿이 먼저 받음  -->
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>/WEB-INF/spring/servlet-context.xml</param-value>
        </init-param>
        <!-- 서버가 켜질 때 이 서블릿을 메모리에 로딩  -->
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>appServlet</servlet-name>
        <!-- 모든 요청을 이 서블릿이 받음  -->
        <!-- 브라우저가 /meeting_summary/ 요청 → DispatcherServlet이 받음 
        → 어느 @Controller에 보내야 할지 찾고 → JSP로 포워딩  -->
        <url-pattern>/</url-pattern>
    </servlet-mapping>

</web-app>
